import Vue from 'vue';
import Vuex from 'vuex';
import '../assets/scss/main.scss';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
});
